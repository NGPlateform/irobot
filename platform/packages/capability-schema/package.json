{
  "name": "@irobot/capability-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capability Manifest v0.1 — 版本化设备能力声明，而非直接映射硬件函数。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@irobot/policy-contract": "workspace:*",
    "zod": "^3.24.0"
  }
}
